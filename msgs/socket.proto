package gazebo.swarm.msgs;

/// \ingroup swarm_msgs
/// \interface Socket
/// \brief A message representing a UDP socket.
message Socket
{
  required string address = 1 [default = "broadcast"];
  optional uint32 port    = 2 [default = 4100];
}
